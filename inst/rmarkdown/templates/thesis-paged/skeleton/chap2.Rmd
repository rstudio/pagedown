# Understanding the method to the madness {.chapter}

Need formula's? Here's some `mathjax` notation:

$$
\beta = \sum^{1 - k}\frac{\delta D}{\sum\frac{\Delta \gamma A}{X-i}}
$$

## Adding Tables

You can easyly add tables to this document like so, Table \@ref(tab:atable):

```{r atable}
knitr::kable(iris[1:4,1:5], align = 'c',caption = 'A caption for the table.',col.names = c("Sepal L","Sepal W", "Petal L", "Petal W", "Species"))
```

Here is the abbreviation for my name: <abbr title="Brent Thorne">BT</abbr>
